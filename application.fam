# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="ghost_esp",
    name="Ghost ESP",
    apptype=FlipperAppType.EXTERNAL, # type: ignore
    entry_point="ghost_esp_app",
    stack_size=6 * 1024,
    fap_category="GPIO",
    # Optional values
    fap_version="1.1.9",
    fap_icon="ghost_esp.png",
    fap_icon_assets="images",
)
